config BR2_PACKAGE_LIBMEDIASCAN
	bool "libmediascan"
	select BR2_PACKAGE_LIBEXIF
	
	help
          libmediascan is an evolution of the work done on the Perl
          modules Audio::Scan and Image::Scale

	  https://github.com/andygrundman/libmediascan
